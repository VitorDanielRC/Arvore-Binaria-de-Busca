# Árvore Binária de Busca – Visualização Web (Flask + Canvas)

Este projeto implementa uma **Árvore Binária de Busca (BST) balanceada por reconstrução** em Python (usando Flask) com **visualização gráfica no navegador** através de HTML5 `<canvas>` e JavaScript.

O usuário informa uma lista de números inteiros e pode realizar operações de:

- Construção da árvore a partir de uma lista inicial
- Inserção de novos valores
- Busca + remoção
- Remoção direta
- Visualização gráfica da árvore (nós e arestas) e da lista em ordem (percurso in-order)

---

## 🧩 Tecnologias Utilizadas

- **Python 3.x**
- **Flask** (backend / API)
- **HTML + CSS + JavaScript**
- **Canvas 2D** para desenhar a árvore no navegador

---

## 📁 Estrutura de Arquivos (sugestão)

```bash
projeto-arvore/
├─ app.py
├─ templates/
│  └─ index.html
└─ README.md
